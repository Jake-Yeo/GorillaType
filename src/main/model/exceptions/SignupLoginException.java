package model.exceptions;

// Represents an exception that should be thrown if signup of login fails
public class SignupLoginException extends Exception {
    public SignupLoginException() {
        super();
    }
}
